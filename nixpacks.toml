[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "pip3 install --break-system-packages --upgrade pip",
  "pip3 install --break-system-packages python-telegram-bot numpy aiohttp websockets requests pycryptodome pytz"
]

[start]
cmd = "python3 bot.py"
