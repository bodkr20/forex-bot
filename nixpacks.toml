[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "pip install --break-system-packages --upgrade pip",
  "pip install --break-system-packages python-telegram-bot==20.8 numpy aiohttp websockets requests pycryptodome pytz"
]

[start]
cmd = "python bot.py"
